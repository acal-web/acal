import 'package:flutter/material.dart';

class CashboxPage extends StatelessWidget {
  const CashboxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Caixa'));
  }
}
