server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # go_router uses path-based URLs; unknown paths fall back to index.html
    # so the client-side router can handle them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
