# Flutter engine, Firebase, and flutter_local_notifications ship their own
# consumer ProGuard rules bundled in their AARs, so no manual keep rules are
# required here. Add project-specific rules below as needed.
